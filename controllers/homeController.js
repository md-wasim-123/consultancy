import formmodel from "../models/formSchema.js"

class scholar {
    static homeController = (req, res) => {
        res.render('index')
    }
    static EnquiryData=()=>{
        
    }
}

export default scholar 